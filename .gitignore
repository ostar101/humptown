# Godot 4
.godot/
/android/
export_presets.cfg
*.translation

# Build output
build/
dist/
*.exe
*.pck
*.zip

# Secrets - never commit. Runtime keys live in user:// (see DECISIONS.md D-006)
*.key
secrets.*
.env
.env.*

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Transfer artefacts: the history bundle and its instructions travel beside
# the working tree because the desktop bridge cannot write into .git.
*.bundle
RESTORE_GIT.md

# Local Claude memory MCP server and its registration. Machine-specific
# (absolute paths, a venv) and personal, so never versioned. See CLAUDE.md.
Humptown-Claude-Memory/
.mcp.json
